[package]
name = "kafka-client"
version = "0.1.0"
edition = "2024"

[dependencies]

[dev-dependencies]
testcontainers = "0.27"
testcontainers-modules = { version = "0.15", features = ["kafka"] }
tokio = { version = "1", features = ["full"] }
